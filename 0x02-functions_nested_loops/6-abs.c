#include "main.h"
/**
 * _abs - function that compute the absolute value of an integer
 * @c: is the int for argument of a function
 * Return: 0
 */
int _abs(int c)
{
	if (c . 0 || c == 0)
	{
		return (c);
	}
}
