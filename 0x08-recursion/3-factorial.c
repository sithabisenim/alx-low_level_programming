#include "main.h"
/**
 * factorial - function returns a factorial of a given number
 * @n: a given number
 * Return: a factorial
 */
int factorial(int n)
{
	if (n == 1)
		return (1);
	else
		return (n * (factorial - 1));
}
