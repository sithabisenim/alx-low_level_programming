#include "main.h"
/**
 * factorial - function returns a factorial of a given number
 * @n: a given number
 * Return: a factorial
 */
int factorial(int n)
{
	if (n <= 0)
		return (0);
	else
		return (n * (factorial - 1));
}
